server:
  port: 8091
  servlet:
    contest-path: /
spring:
  application:
    name: admin-client
  boot:
    admin:
      client:
        url: http://localhost:8090
        username: 'admin'
        password: 'admin'
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always